<Window x:Class="IvanaDrugi.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Frizerski Salon" Height="600" Width="900"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        FontFamily="{DynamicResource MainFont}">
    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="{DynamicResource MenuBackground}" Foreground="{DynamicResource MenuForeground}">
            <MenuItem Header="Usluge" x:Name="menuUsluge" Foreground="{DynamicResource MenuForeground}">
                <MenuItem Header="Pregled usluga" Click="BtnPregledUsluga_Click"/>
                <MenuItem Header="Dodaj novu uslugu" Click="BtnDodajUslugu_Click" x:Name="itemDodajUslugu"/>
               
                <MenuItem Header="Obriši uslugu" Click="BtnObrisiUslugu_Click" x:Name="itemObrisiUslugu"/>
            </MenuItem>

            <MenuItem Header="Rezervacije" x:Name="menuRezervacije" Foreground="{DynamicResource MenuForeground}">
                <MenuItem Header="Kreiraj rezervaciju" Click="BtnKreirajRezervaciju_Click"/>
                <MenuItem Header="Zakazane" Click="BtnZakazane_Click"/>
                <MenuItem Header="Pregled rezervacija" Click="BtnPregledRezervacija_Click"/>
            </MenuItem>

            <MenuItem Header="Računi" x:Name="menuRacuni" Foreground="{DynamicResource MenuForeground}">
             
                <MenuItem Header="Pregled računa" Click="BtnPregledRacuna_Click"/>
                <MenuItem Header="Pretraga računa po datumu" Click="BtnPretragaRacuna_Click"/>
            </MenuItem>

            <MenuItem Header="Postavke" x:Name="menuPostavke" Foreground="{DynamicResource MenuForeground}">
                <MenuItem Header="Upravljanje korisnicima" Click="BtnUpravljanjeKorisnicima_Click"/>
                <MenuItem Header="Promjena teme" Click="BtnPromjenaTeme_Click"/>
                <MenuItem Header="Promjena jezika" Click="BtnPromjenaJezika_Click"/>
            </MenuItem>
        </Menu>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10">
            <Button Content="➕ Dodaj korisnika" 
                    Click="BtnDodajKorisnika_Click"
                    Padding="12,6"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Cursor="Hand"
                    BorderBrush="Transparent"
                    x:Name="btnDodajKorisnika"/>
        </StackPanel>

        <ContentControl x:Name="MainContent" Margin="20"/>
    </DockPanel>
</Window>
<Window x:Class="IvanaDrugi.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prijava - Frizerski Salon" 
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        FontFamily="{DynamicResource MainFont}">

    <Border Margin="20" Padding="20" Background="{DynamicResource BackgroundBrush}" CornerRadius="15" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="2">
        <StackPanel>
            <TextBlock x:Name="txtSalonName"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,0,0,20"/>

            <TextBlock x:Name="txtUsernameLabel"
                       Margin="0,0,0,5"
                       Foreground="{DynamicResource ForegroundBrush}"/>

            <TextBox x:Name="txtUsername"
                     Height="35"
                     FontSize="14"
                     Padding="8"
                     BorderBrush="{DynamicResource AccentBrush}"
                     BorderThickness="1"
                     Background="{DynamicResource BackgroundBrush}"
                     Foreground="{DynamicResource ForegroundBrush}"/>

            <TextBlock x:Name="txtPasswordLabel"
                       Margin="0,15,0,5"
                       Foreground="{DynamicResource ForegroundBrush}"/>

            <PasswordBox x:Name="txtPassword"
                         Height="35"
                         FontSize="14"
                         Padding="8"
                         BorderBrush="{DynamicResource AccentBrush}"
                         BorderThickness="1"
                         Background="{DynamicResource BackgroundBrush}"
                         Foreground="{DynamicResource ForegroundBrush}"/>

            <Button x:Name="btnLogin"
                    Click="BtnLogin_Click"
                    Height="40"
                    FontSize="16"
                    Margin="0,25,0,0"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderBrush="Transparent"
                    Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Border>
</Window>
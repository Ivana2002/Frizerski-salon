<UserControl x:Class="IvanaDrugi.Views.UslugeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtNaslov" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource ForegroundBrush}"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,15">
            <Button x:Name="btnDodajUslugu" 
                    Click="BtnDodajUslugu_Click" 
                    Margin="0,0,20,0"
                    Padding="12,6"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    FontSize="14"
                    Cursor="Hand"
                    BorderBrush="Transparent">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="txtPretragaLabel" Margin="0,0,10,0" Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBox x:Name="txtPretraga" Height="30" Width="200" KeyUp="TxtPretraga_KeyUp"
                         Foreground="{DynamicResource ForegroundBrush}"
                         Background="{DynamicResource BackgroundBrush}"
                         BorderBrush="{DynamicResource AccentBrush}"/>
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="DataGridUsluge"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Foreground="{DynamicResource ForegroundBrush}"
                  Background="{DynamicResource BackgroundBrush}"
                  RowBackground="{DynamicResource BackgroundBrush}"
                  AlternatingRowBackground="{DynamicResource BackgroundBrush}"
                  BorderBrush="{DynamicResource AccentBrush}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Šifra" Binding="{Binding Sifra}" Width="80"/>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="150"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis, TargetNullValue='Nema opisa'}" Width="200"/>
                <DataGridTextColumn Header="Cijena (KM)" Binding="{Binding Cena, StringFormat={}{0:N2} KM}" Width="100"/>
                <DataGridTextColumn Header="Trajanje (min)" Binding="{Binding TrajanjeMinuta}" Width="100"/>
                <DataGridTemplateColumn Header="Akcija" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="BtnObrisi_Click" 
                                    Background="{DynamicResource AccentBrush}"  
                                    Foreground="White" 
                                    BorderBrush="Transparent"
                                    Cursor="Hand"
                                    Padding="5,3"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Content="❌">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="3" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>